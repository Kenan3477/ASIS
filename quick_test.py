#!/usr/bin/env python3import asynciofrom asis_cross_domain_reasoning_engine import CrossDomainReasoningEngineasync def main():    print("Testing Cross-Domain Interface...")    engine = CrossDomainReasoningEngine()        try:        result = await engine.reason_across_domains(            "How can biological evolution principles improve AI algorithms?",             "biology",             "computer_science"        )        print(f"✅ SUCCESS: Confidence {result.get('reasoning_confidence', 0):.2f}")        print(f"Domains: {result.get('domains_analyzed', [])}")        print(f"Principles: {len(result.get('transferred_principles', []))}")    except Exception as e:        print(f"❌ ERROR: {e}")asyncio.run(main())